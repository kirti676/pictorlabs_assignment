# Environment Configuration
ENV=dev
BASE_URL=https://development.prism.deepstain.com/

# Authentication
# NOTE: For CI/CD pipelines, configure these as environment variables or secrets:
# - GitHub Actions: Use repository secrets (Settings > Secrets and variables > Actions)
# - GitLab CI: Use CI/CD variables (Settings > CI/CD > Variables, mark as 'Masked')
# - Jenkins: Use credentials binding or environment variables in pipeline configuration
# - Azure DevOps: Use pipeline variables (mark as secret)
# Pipeline environment variables will automatically override these hardcoded values
TEST_USERNAME=pictor.newqa.guest@pictorlabs.ai
TEST_PASSWORD=MUT!RHrc6a!@4Fp

# Browser Configuration
BROWSER=chromium
HEADLESS=true
SLOW_MO=0
TIMEOUT=30000

# Test Configuration
PARALLEL_WORKERS=3
RETRY_COUNT=1

# Screenshot & Video
SCREENSHOT_ON_FAILURE=true
VIDEO_ON_FAILURE=true

# Reporting
ALLURE_RESULTS_DIR=./reports/allure-results
